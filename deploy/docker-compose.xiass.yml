# =============================================================================
# XIASS API - GHCR 镜像覆盖文件（配合 docker-compose.local.yml 使用）
# =============================================================================
# 用法：
#   docker compose -f docker-compose.local.yml -f docker-compose.xiass.yml up -d
#   docker compose -f docker-compose.local.yml -f docker-compose.xiass.yml pull
# =============================================================================
services:
  xiass-api:
    image: ghcr.io/xyf0104/xiass-api:latest
